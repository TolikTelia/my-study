import React from 'react';

export const sum = (a, b) => a + b;

export const assigner  = (obj, prop, val) => {
    obj[prop] = val
};

const Tests = () => {
    return (
        <div className={'tests'}>
            
        </div>
    );
};

export default Tests;